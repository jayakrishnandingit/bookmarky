#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat 'echo "Hello World!"'
                bat 'py --version'
            }
        }
    }
}
